# iterable = A collection\object that can return its elements one at a time
# we can use it in a loop

# these include lists, tuples ,sets, string, dictionary
# note: when we iterate over dictionary it returns keys

fruits={
    "a":"apple",
    "b":"banana"
}

for fruit in fruits.values():
    print(fruit)
    
